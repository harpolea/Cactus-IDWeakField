#!/bin/bash
#PBS -S /bin/bash

# PBS job options (compute nodes, job time)
#PBS -l nodes=1:ppn=16
#PBS -l walltime=04:00:00

# Change to the directory that the job was submitted from
cd $PBS_O_WORKDIR

# load openmpi
module load openmpi

# Launch the parallel job
mpirun exe/cactus_sim $PBS_O_WORKDIR/arrangements/EinsteinInitialData/IDWeakField/par/weak_field.par
